import streamlit as st
import pandas as pd
import re

# ตั้งค่าหน้าเว็บให้กว้างและดูง่าย
st.set_page_config(page_title="ระบบสรุปงาน Padlet ม.3", layout="wide")

st.title("📋 ระบบสรุปการส่งงานชีววิทยา (Padlet)")
st.markdown("---")

# ฟังก์ชันจัดการชื่อ (นำคำนำหน้าออกและแยกชื่อ-นามสกุล)
def split_name_parts(full_name_text):
    # รายการคำนำหน้าที่จะตัดออก
    prefixes = [r'^นาย', r'^นางสาว', r'^ด\.ช\.', r'^ด\.ญ\.', r'^เด็กชาย', r'^เด็กหญิง', r'^ดช\.', r'^ดญ\.']
    cleaned = str(full_name_text).strip()
    for p in prefixes:
        cleaned = re.sub(p, '', cleaned).strip()
    
    # แยกชื่อกับนามสกุลด้วยช่องว่าง
    parts = cleaned.split(maxsplit=1)
    first_name = parts[0] if len(parts) > 0 else "-"
    last_name = parts[1] if len(parts) > 1 else "-"
    return first_name, last_name

# ฟังก์ชันดึงชื่อกลุ่ม (เช่น กลุ่มที่ 1 NumberOne)
def get_full_group(section_text):
    text = str(section_text)
    # ค้นหาคำว่า "กลุ่มที่ X"
    num_match = re.search(r'(กลุ่มที่\s*\d+)', text)
    # ค้นหาชื่อกลุ่มที่อยู่หลังวงเล็บ )
    name_match = re.search(r'\)\s*(.*)', text)
    
    g_num = num_match.group(1) if num_match else "ไม่ระบุกลุ่ม"
    g_name = name_match.group(1).strip() if name_match else ""
    return f"{g_num} {g_name}".strip()

# ส่วนอัปโหลดไฟล์
uploaded_file = st.file_uploader("อัปโหลดไฟล์ 'โพสต์.csv' จาก Padlet", type=["csv"])

if uploaded_file:
    try:
        # อ่านไฟล์ CSV โดยใช้ utf-8-sig เพื่อรองรับภาษาไทยจาก Excel/Padlet
        df = pd.read_csv(uploaded_file, encoding='utf-8-sig')
        
        # ล้างช่องว่างที่ชื่อคอลัมน์
        df.columns = [c.strip() for c in df.columns]
        
        # กรองโพสต์ของคุณครู (ตระกูล บุญชิต) ออกจากรายชื่อส่งงาน
        if 'ผู้เขียน' in df.columns:
            df = df[~df['ผู้เขียน'].str.contains("ตระกูล", na=False)]
        
        all_data = []
        for _, row in df.iterrows():
            # รวม 'เรื่อง' และ 'เนื้อหา' เพื่อหาเลขที่และกิจกรรม
            subject = str(row.get('เรื่อง', ''))
            content = str(row.get('เนื้อหา', ''))
            full_text = f"{subject} {content}"
            
            # 1. ค้นหาเลขที่
            no_match = re.search(r'เลขที่\s*(\d+)', full_text)
            student_no = no_match.group(1) if no_match else "-"
            
            # 2. ค้นหาชื่อ-นามสกุล (ถ้ามีคำนำหน้าในโพสต์ให้ใช้ ถ้าไม่มีให้ใช้ชื่อผู้เขียน)
            name_match = re.search(r'(นาย|นางสาว|ด\.ช\.|ด\.ญ\.|เด็กชาย|เด็กหญิง)\s*([^\s\d]+)\s+([^\s\d]+)', full_text)
            if name_match:
                full_raw_name = name_match.group(0)
            else:
                full_raw_name = str(row.get('ผู้เขียน', 'Unknown')).split('(')[0].strip()
            
            fname, lname = split_name_parts(full_raw_name)
            
            # 3. ชื่อกลุ่มและกิจกรรม
            group_display = get_full_group(row.get('ส่วน', ''))
            act_match = re.search(r'กิจกรรมที่\s*(\d+\.?\d*)', full_text)
            activity_id = act_match.group(1) if act_match else None
            
            all_data.append({
                'เลขที่': student_no,
                'ชื่อ': fname,
                'นามสกุล
